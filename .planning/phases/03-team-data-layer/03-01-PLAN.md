---
phase: 03-team-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamDetails.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamResult.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamResultList.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecord.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecordStatOption.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecordRange.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/TeamService.kt
  - domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/repository/TeamRepository.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamDetailsDto.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamResultDto.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamResultListDto.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamRosterDto.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamRecordDto.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamRecordListDto.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkTeamService.kt
  - data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/TeamRepositoryImpl.kt
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03

must_haves:
  truths:
    - "TeamService can fetch team details, results, roster, and records from the API"
    - "All DTOs correctly deserialize JSON and map to domain models"
    - "TeamRepository delegates all calls to TeamService without transformation"
  artifacts:
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamDetails.kt"
      provides: "TeamDetails domain model (id, name, logoUrl, seasons, league)"
      contains: "data class TeamDetails"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamResult.kt"
      provides: "TeamResult domain model (id, opponent, points, date, status, type)"
      contains: "data class TeamResult"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamResultList.kt"
      provides: "TeamResultList wrapper (data, league)"
      contains: "data class TeamResultList"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecord.kt"
      provides: "TeamRecord domain model (player, value, position, games)"
      contains: "data class TeamRecord"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecordStatOption.kt"
      provides: "Enum of 9 stat categories with apiKey"
      contains: "enum class TeamRecordStatOption"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecordRange.kt"
      provides: "Enum of 3 ranges with apiKey + buildRecordCategory helper"
      contains: "enum class TeamRecordRange"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/TeamService.kt"
      provides: "TeamService interface with 4 suspend methods"
      contains: "interface TeamService"
    - path: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/repository/TeamRepository.kt"
      provides: "TeamRepository interface mirroring TeamService"
      contains: "interface TeamRepository"
    - path: "data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkTeamService.kt"
      provides: "NetworkTeamService implementing 4 API endpoints"
      contains: "class NetworkTeamService"
    - path: "data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/TeamRepositoryImpl.kt"
      provides: "TeamRepositoryImpl delegating to TeamService"
      contains: "class TeamRepositoryImpl"
  key_links:
    - from: "data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkTeamService.kt"
      to: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/TeamService.kt"
      via: "implements interface"
      pattern: "class NetworkTeamService.*: TeamService"
    - from: "data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/TeamRepositoryImpl.kt"
      to: "domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/repository/TeamRepository.kt"
      via: "implements interface"
      pattern: "class TeamRepositoryImpl.*: TeamRepository"
    - from: "data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamResultDto.kt"
      to: "data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/MatchTeamDto.kt"
      via: "reuses existing MatchTeamDto for opponent field"
      pattern: "MatchTeamDto"
---

<objective>
Create the complete team data layer: domain models, DTOs with toDomain() mappers, TeamService interface + NetworkTeamService implementation, and TeamRepository interface + TeamRepositoryImpl for all 4 team API endpoints.

Purpose: Establish the data pipeline (API -> DTO -> Domain Model -> Repository) needed by the TeamDetails ViewModel and Screen in Plan 02, matching the Phase 1 Player Data Layer structure exactly.

Output: 6 domain models, 6 DTOs, TeamService/NetworkTeamService, TeamRepository/TeamRepositoryImpl — all compiling against existing project patterns.
</objective>

<execution_context>
@/Users/marcinzielinski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcinzielinski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-team-data-layer/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/Team.kt:
```kotlin
package com.mzs.basket_krk.domain.model
data class Team(val id: Int, val name: String, val logoUrl: String)
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/Season.kt:
```kotlin
package com.mzs.basket_krk.domain.model
data class Season(val id: Int, val num: Int)
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/League.kt:
```kotlin
package com.mzs.basket_krk.domain.model
data class League(val id: Int, val name: String)
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/MatchTeam.kt:
```kotlin
package com.mzs.basket_krk.domain.model
data class MatchTeam(val id: Int, val name: String, val shortName: String, val logoUrl: String, val points: Int)
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/MatchStatus.kt:
```kotlin
enum class MatchStatus(val key: String) {
    NON_STARTED("ns"), IN_PROGRESS("ip"), FINISHED("me"), WALKOVER("wo");
    companion object { fun fromKey(keyName: String): MatchStatus = ... }
}
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/MatchType.kt:
```kotlin
enum class MatchType(val key: String) {
    REGULAR_SEASON("rs"), PLAYOFFS("po");
    companion object { fun fromKey(keyName: String): MatchType = ... }
}
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/PlayerShort.kt:
```kotlin
package com.mzs.basket_krk.domain.model
data class PlayerShort(val id: Int, val name: String)
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/PlayerWithStat.kt:
```kotlin
package com.mzs.basket_krk.domain.model
data class PlayerWithStat(val player: PlayerShort, val stat: Stat)
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/PlayerService.kt (pattern to mirror):
```kotlin
interface PlayerService {
    suspend fun getPlayerDetails(playerId: Int): Either<Failure, PlayerDetails>
    suspend fun getPlayerGameLogs(playerId: Int, seasonId: Int): Either<Failure, PlayerLogList>
    suspend fun getPlayerStats(playerId: Int): Either<Failure, List<PlayerStat>>
    suspend fun getPlayerRecords(playerId: Int): Either<Failure, List<PlayerRecord>>
}
```

From domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/base/EitherExtensions.kt:
```kotlin
inline fun <T> Either.Companion.catchWithError(f: () -> T): Either<Failure, T> = ...
```

From data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/SeasonDto.kt:
```kotlin
@Serializable data class SeasonDto(val id: Int, val num: Int)
fun SeasonDto.toDomain() = Season(id = id, num = num)
```

From data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/LeagueDto.kt:
```kotlin
@Serializable data class LeagueDto(val id: Int, val name: String)
fun LeagueDto.toDomain() = League(id = id, name = name)
```

From data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/MatchTeamDto.kt:
```kotlin
@Serializable data class MatchTeamDto(val id: Int, val name: String, val s_name: String, val logo: String, val pts: Int)
fun MatchTeamDto.toDomain() = MatchTeam(id = id, name = name, shortName = s_name, logoUrl = logo, points = pts)
```

From data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/PlayerWithStatDto.kt:
```kotlin
@Serializable data class PlayerWithStatDto(val player: PlayerShortDto, val stat: StatDto)
fun PlayerWithStatDto.toDomain() = PlayerWithStat(player = player.toDomain(), stat = stat.toDomain())
```

From data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkPlayerService.kt (pattern to mirror):
```kotlin
class NetworkPlayerService(private val apiService: ApiService) : PlayerService {
    override suspend fun getPlayerDetails(playerId: Int): Either<Failure, PlayerDetails> {
        return Either.catchWithError { apiService.get<PlayerDetailsDto>("/player/$playerId/").toDomain() }
    }
    // ... 3 more methods following same pattern
}
```

From data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/PlayerRepositoryImpl.kt (pattern to mirror):
```kotlin
class PlayerRepositoryImpl(private val playerService: PlayerService) : PlayerRepository {
    override suspend fun getPlayerDetails(playerId: Int) = playerService.getPlayerDetails(playerId)
    // ... delegates all methods
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain models and enums for team data</name>
  <files>
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamDetails.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamResult.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamResultList.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecord.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecordStatOption.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/TeamRecordRange.kt
  </files>
  <read_first>
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/Team.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/Season.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/League.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/MatchTeam.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/MatchStatus.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/MatchType.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/PlayerShort.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/PlayerWithStat.kt
  </read_first>
  <action>
Create 6 new domain model files in `domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/model/`. Package: `com.mzs.basket_krk.domain.model`.

**TeamDetails.kt:**
```kotlin
data class TeamDetails(
    val id: Int,
    val name: String,
    val logoUrl: String,
    val seasons: List<Season>,
    val league: League?
)
```
Reuses existing `Season` and `League` models. `league` is nullable because the API field `last_league` can be null.

**TeamResult.kt:**
```kotlin
data class TeamResult(
    val id: Int,
    val opponent: MatchTeam,
    val points: Int,
    val date: String,
    val status: MatchStatus,
    val type: MatchType
)
```
Reuses existing `MatchTeam`, `MatchStatus`, `MatchType`. `opponent` contains the opposing team's id/name/shortName/logoUrl/points. `points` is this team's score.

**TeamResultList.kt:**
```kotlin
data class TeamResultList(
    val data: List<TeamResult>,
    val league: League
)
```
Wrapper matching API response shape. `league` is non-nullable (always present when results exist). Reuses existing `League`.

**TeamRecord.kt:**
```kotlin
data class TeamRecord(
    val player: PlayerShort,
    val value: Int,
    val position: Int,
    val games: Int,
    val ats: Int?,
    val sNum: Int?,
    val matchId: Int?
)
```
Reuses existing `PlayerShort(id, name)`. `ats`, `sNum`, `matchId` are nullable optional fields from the API.

**TeamRecordStatOption.kt:**
```kotlin
enum class TeamRecordStatOption(val apiKey: String) {
    PTS("pts"), AST("ast"), REB("reb"), STL("stl"), BLK("blk"),
    EFF("eff"), FT("ft"), FG("fg"), FG3("fg3")
}
```
9 stat categories. `apiKey` is the lowercase string used in the composite `cat` parameter.

**TeamRecordRange.kt:**
```kotlin
enum class TeamRecordRange(val apiKey: String) {
    ALL_TIME("t"), SEASON("s"), MATCH("m")
}

fun buildRecordCategory(stat: TeamRecordStatOption, range: TeamRecordRange): String =
    "${stat.apiKey}_${range.apiKey}"
```
3 range options. The `buildRecordCategory` top-level function builds the composite string (e.g., "pts_t" for PTS All-Time). Default first call uses `TeamRecordStatOption.PTS` + `TeamRecordRange.ALL_TIME`.
  </action>
  <verify>
    <automated>cd /Users/marcinzielinski/Documents/Development/kmp/basket-krk-kmp && ./gradlew :domain:compileCommonMainKotlinMetadata 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "data class TeamDetails" domain/src/commonMain/ returns match with fields: id, name, logoUrl, seasons, league
    - grep -r "data class TeamResult(" domain/src/commonMain/ returns match with fields: id, opponent, points, date, status, type
    - grep -r "data class TeamResultList" domain/src/commonMain/ returns match with fields: data, league
    - grep -r "data class TeamRecord" domain/src/commonMain/ returns match with fields: player, value, position, games, ats, sNum, matchId
    - grep -r "enum class TeamRecordStatOption" domain/src/commonMain/ returns match with PTS, AST, REB, STL, BLK, EFF, FT, FG, FG3
    - grep -r "enum class TeamRecordRange" domain/src/commonMain/ returns match with ALL_TIME, SEASON, MATCH
    - grep -r "fun buildRecordCategory" domain/src/commonMain/ returns match
    - ./gradlew :domain:compileCommonMainKotlinMetadata succeeds with no errors
  </acceptance_criteria>
  <done>6 domain model files exist and compile. TeamDetails uses Season/League, TeamResult uses MatchTeam/MatchStatus/MatchType, TeamRecord uses PlayerShort, both enums have apiKey properties, buildRecordCategory helper is available.</done>
</task>

<task type="auto">
  <name>Task 2: Create DTOs with toDomain() mappers and TeamService/TeamRepository interfaces</name>
  <files>
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamDetailsDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamResultDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamResultListDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamRosterDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamRecordDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/TeamRecordListDto.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/TeamService.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/repository/TeamRepository.kt
  </files>
  <read_first>
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/SeasonDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/LeagueDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/MatchTeamDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/PlayerWithStatDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/PlayerShortDto.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/SearchResultDto.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/PlayerService.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/repository/PlayerRepository.kt
  </read_first>
  <action>
Create 6 DTO files in `data/src/commonMain/kotlin/com/mzs/basket_krk/data/dto/` and 2 interface files in domain. Package for DTOs: `com.mzs.basket_krk.data.dto`. Package for interfaces: `com.mzs.basket_krk.domain.service` and `com.mzs.basket_krk.domain.repository`.

**TeamDetailsDto.kt:**
```kotlin
@Serializable
data class TeamDetailsDto(
    val id: Int,
    val name: String,
    val logo: String,
    val seasons: List<SeasonDto>,
    val last_league: LeagueDto? = null
)

fun TeamDetailsDto.toDomain() = TeamDetails(
    id = id,
    name = name,
    logoUrl = logo,
    seasons = seasons.map { it.toDomain() },
    league = last_league?.toDomain()
)
```
Reuses existing `SeasonDto.toDomain()` and `LeagueDto.toDomain()`. `last_league` is nullable with default null.

**TeamResultDto.kt:**
```kotlin
@Serializable
data class TeamResultDto(
    val id: Int,
    val opp: MatchTeamDto,
    val pts: Int,
    val date: String,
    val status: String,
    val type: String
)

fun TeamResultDto.toDomain() = TeamResult(
    id = id,
    opponent = opp.toDomain(),
    points = pts,
    date = date,
    status = MatchStatus.fromKey(status),
    type = MatchType.fromKey(type)
)
```
Reuses existing `MatchTeamDto.toDomain()`. Uses `MatchStatus.fromKey()` and `MatchType.fromKey()` for enum conversion.

**TeamResultListDto.kt:**
```kotlin
@Serializable
data class TeamResultListDto(
    val data: List<TeamResultDto>,
    val lg: LeagueDto
)

fun TeamResultListDto.toDomain() = TeamResultList(
    data = data.map { it.toDomain() },
    league = lg.toDomain()
)
```
`lg` is the API field name for league. Reuses existing `LeagueDto.toDomain()`.

**TeamRosterDto.kt:**
```kotlin
@Serializable
data class TeamRosterDto(val data: List<PlayerWithStatDto>)

fun TeamRosterDto.toDomain(): List<PlayerWithStat> = data.map { it.toDomain() }
```
Envelope DTO wrapping existing `PlayerWithStatDto`. Reuses existing `PlayerWithStatDto.toDomain()` which maps to `PlayerWithStat(player: PlayerShort, stat: Stat)`.

**TeamRecordDto.kt:**
```kotlin
@Serializable
data class TeamRecordDto(
    val player: PlayerInRecordDto,
    val value: Int,
    val position: Int,
    val games: Int,
    val ats: Int? = null,
    val s_num: Int? = null,
    val match_id: Int? = null
)

@Serializable
data class PlayerInRecordDto(
    val id: Int,
    val fn: String,
    val ln: String,
    val t: TeamDto? = null
)

fun TeamRecordDto.toDomain() = TeamRecord(
    player = PlayerShort(id = player.id, name = "${player.fn} ${player.ln}"),
    value = value,
    position = position,
    games = games,
    ats = ats,
    sNum = s_num,
    matchId = match_id
)
```
IMPORTANT: `PlayerInRecordDto` uses the full player shape (id, fn, ln, optional t) NOT `PlayerShortDto` (id, name). The API sends fn/ln separately for records. Import `TeamDto` from `SearchResultDto.kt` for the optional team field. The `t` field has `= null` default so it parses safely whether present or absent.

**TeamRecordListDto.kt:**
```kotlin
@Serializable
data class TeamRecordListDto(val data: List<TeamRecordDto>)

fun TeamRecordListDto.toDomain(): List<TeamRecord> = data.map { it.toDomain() }
```
Envelope DTO wrapping the records list.

**TeamService.kt** (domain/service):
```kotlin
interface TeamService {
    suspend fun getTeamDetails(teamId: Int): Either<Failure, TeamDetails>
    suspend fun getTeamResults(teamId: Int, seasonId: Int): Either<Failure, TeamResultList>
    suspend fun getTeamRoster(teamId: Int, seasonId: Int): Either<Failure, List<PlayerWithStat>>
    suspend fun getTeamRecords(teamId: Int, category: String): Either<Failure, List<TeamRecord>>
}
```
Mirror of PlayerService pattern. 4 suspend methods. `category` is the pre-built composite string from `buildRecordCategory()`.

**TeamRepository.kt** (domain/repository):
```kotlin
interface TeamRepository {
    suspend fun getTeamDetails(teamId: Int): Either<Failure, TeamDetails>
    suspend fun getTeamResults(teamId: Int, seasonId: Int): Either<Failure, TeamResultList>
    suspend fun getTeamRoster(teamId: Int, seasonId: Int): Either<Failure, List<PlayerWithStat>>
    suspend fun getTeamRecords(teamId: Int, category: String): Either<Failure, List<TeamRecord>>
}
```
Mirrors TeamService exactly (same as PlayerRepository mirrors PlayerService).
  </action>
  <verify>
    <automated>cd /Users/marcinzielinski/Documents/Development/kmp/basket-krk-kmp && ./gradlew :data:compileCommonMainKotlinMetadata 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "data class TeamDetailsDto" data/src/commonMain/ returns match
    - grep -r "fun TeamDetailsDto.toDomain" data/src/commonMain/ returns match
    - grep -r "data class TeamResultDto" data/src/commonMain/ returns match with fields: opp, pts, date, status, type
    - grep -r "data class TeamResultListDto" data/src/commonMain/ returns match with fields: data, lg
    - grep -r "data class TeamRosterDto" data/src/commonMain/ returns match
    - grep -r "data class TeamRecordDto" data/src/commonMain/ returns match with fields: player, value, position, games
    - grep -r "data class PlayerInRecordDto" data/src/commonMain/ returns match with fields: id, fn, ln
    - grep -r "data class TeamRecordListDto" data/src/commonMain/ returns match
    - grep -r "interface TeamService" domain/src/commonMain/ returns match with 4 suspend fun methods
    - grep -r "interface TeamRepository" domain/src/commonMain/ returns match with 4 suspend fun methods
    - ./gradlew :data:compileCommonMainKotlinMetadata succeeds with no errors
  </acceptance_criteria>
  <done>6 DTO files with toDomain() mappers compile correctly. TeamService and TeamRepository interfaces defined in domain. DTOs reuse existing SeasonDto, LeagueDto, MatchTeamDto, PlayerWithStatDto. PlayerInRecordDto handles the full player shape (fn/ln) distinct from PlayerShortDto.</done>
</task>

<task type="auto">
  <name>Task 3: Create NetworkTeamService and TeamRepositoryImpl</name>
  <files>
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkTeamService.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/TeamRepositoryImpl.kt
  </files>
  <read_first>
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkPlayerService.kt,
    data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/PlayerRepositoryImpl.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/service/TeamService.kt,
    domain/src/commonMain/kotlin/com/mzs/basket_krk/domain/repository/TeamRepository.kt
  </read_first>
  <action>
Create 2 implementation files. Mirror NetworkPlayerService and PlayerRepositoryImpl exactly.

**NetworkTeamService.kt** in `data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/`:
Package: `com.mzs.basket_krk.data.service`.

```kotlin
class NetworkTeamService(private val apiService: ApiService) : TeamService {
    override suspend fun getTeamDetails(teamId: Int): Either<Failure, TeamDetails> {
        return Either.catchWithError {
            apiService.get<TeamDetailsDto>("/team/$teamId/").toDomain()
        }
    }

    override suspend fun getTeamResults(teamId: Int, seasonId: Int): Either<Failure, TeamResultList> {
        return Either.catchWithError {
            apiService.get<TeamResultListDto>("/team/$teamId/results?season_id=$seasonId").toDomain()
        }
    }

    override suspend fun getTeamRoster(teamId: Int, seasonId: Int): Either<Failure, List<PlayerWithStat>> {
        return Either.catchWithError {
            apiService.get<TeamRosterDto>("/team/$teamId/players?season_id=$seasonId").toDomain()
        }
    }

    override suspend fun getTeamRecords(teamId: Int, category: String): Either<Failure, List<TeamRecord>> {
        return Either.catchWithError {
            apiService.get<TeamRecordListDto>("/team/$teamId/records?cat=$category").toDomain()
        }
    }
}
```

API paths confirmed from Flutter `team_remote_datasource.dart`:
- `/team/{id}/` for details
- `/team/{id}/results?season_id={id}` for results
- `/team/{id}/players?season_id={id}` for roster
- `/team/{id}/records?cat={category}` for records

Import `Either.catchWithError` from `com.mzs.basket_krk.domain.base.catchWithError`. Import all needed DTOs and their `toDomain` extensions. Import domain models for return types.

**TeamRepositoryImpl.kt** in `data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/`:
Package: `com.mzs.basket_krk.data.repository`.

```kotlin
class TeamRepositoryImpl(private val teamService: TeamService) : TeamRepository {
    override suspend fun getTeamDetails(teamId: Int) = teamService.getTeamDetails(teamId)
    override suspend fun getTeamResults(teamId: Int, seasonId: Int) = teamService.getTeamResults(teamId, seasonId)
    override suspend fun getTeamRoster(teamId: Int, seasonId: Int) = teamService.getTeamRoster(teamId, seasonId)
    override suspend fun getTeamRecords(teamId: Int, category: String) = teamService.getTeamRecords(teamId, category)
}
```

Pure delegation, no transformation. Same pattern as PlayerRepositoryImpl.
  </action>
  <verify>
    <automated>cd /Users/marcinzielinski/Documents/Development/kmp/basket-krk-kmp && ./gradlew :data:compileCommonMainKotlinMetadata 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "class NetworkTeamService" data/src/commonMain/ returns match showing ": TeamService"
    - grep -r "apiService.get" data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkTeamService.kt returns 4 matches (one per endpoint)
    - grep -r "/team/" data/src/commonMain/kotlin/com/mzs/basket_krk/data/service/NetworkTeamService.kt returns 4 API paths
    - grep -r "class TeamRepositoryImpl" data/src/commonMain/ returns match showing ": TeamRepository"
    - grep -r "teamService\." data/src/commonMain/kotlin/com/mzs/basket_krk/data/repository/TeamRepositoryImpl.kt returns 4 delegation calls
    - ./gradlew :data:compileCommonMainKotlinMetadata succeeds with no errors
  </acceptance_criteria>
  <done>NetworkTeamService implements all 4 API endpoints using Either.catchWithError + apiService.get pattern. TeamRepositoryImpl delegates all 4 methods to TeamService. Both compile with no errors. The complete data pipeline from API to domain model is established.</done>
</task>

</tasks>

<verification>
Run full data module compilation to confirm all new code integrates:
```bash
./gradlew :data:compileCommonMainKotlinMetadata
```
This validates domain models, DTOs, service, and repository all compile together.
</verification>

<success_criteria>
- 6 domain model files exist in domain/model/ (TeamDetails, TeamResult, TeamResultList, TeamRecord, TeamRecordStatOption, TeamRecordRange)
- 6 DTO files exist in data/dto/ (TeamDetailsDto, TeamResultDto, TeamResultListDto, TeamRosterDto, TeamRecordDto, TeamRecordListDto)
- TeamService interface with 4 methods exists in domain/service/
- TeamRepository interface with 4 methods exists in domain/repository/
- NetworkTeamService implements all 4 API endpoints in data/service/
- TeamRepositoryImpl delegates all 4 methods in data/repository/
- ./gradlew :data:compileCommonMainKotlinMetadata passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-data-layer/03-01-SUMMARY.md`
</output>
